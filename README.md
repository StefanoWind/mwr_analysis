# Microwave radiometer analysis

This set of codes analyzes sample radiometric data to:
- Explore quality issues in raw radiometric data
- Detect and remove radio frequency interference
- Create a dummy dashboard for instrument control

Installation guide:
1. **Install Git**  
   If you don’t have Git installed, download and install it from [https://git-scm.com/downloads](https://git-scm.com/downloads).

2. **Open a terminal or command prompt**  
   Navigate to the folder where you want to clone the repository.

3. **Clone the repository**  
   Run the following command, replacing the URL with the repository's URL:

   ```bash
   git clone https://github.com/StefanoWind/mwr_analysis.git

4. **Include data**   
   Copy data in the ./data/** folder according to the paths provided in the *Inputs* section of the script.

5. **Install dependencies**   
   Install required Python packages through pip.





